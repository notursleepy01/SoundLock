<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer shadow/border (dark) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/pixel_border_dark" />
        </shape>
    </item>
    <!-- Inner border (bright) -->
    <item
        android:left="3dp"
        android:top="3dp"
        android:right="3dp"
        android:bottom="3dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/pixel_border" />
        </shape>
    </item>
    <!-- Panel background -->
    <item
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/pixel_panel" />
        </shape>
    </item>
</layer-list>
